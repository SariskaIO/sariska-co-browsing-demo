// import { TOGGLE_THEME } from "../actions/types";

// const initialState = {
//     darkMode: JSON.parse(localStorage.getItem('dark-theme')) || true
// }

// export const toggleTheme = (state = initialState, action) => {
//     switch(action.type) {
//         case TOGGLE_THEME:
//             state.darkMode = action.payload;
//             return {...state};
//         default:
//             return state;
//     }
// }
